# @proxi/backend-api

Backend API unico de Proxi. Centraliza los modulos NestJS compartidos por cliente, proveedor, admin y landing.

```bash
pnpm --filter @proxi/backend-api dev
```

Puerto local: `4000`.
