# @proxi/client-web

Aplicación Next.js 15 (App Router) del monorepo Proxi — app para **clientes**.

## Desarrollo

```bash
pnpm --filter @proxi/client-web dev
```

Corre en el puerto **3000**. Consume los paquetes compartidos `@proxi/ui` y `@proxi/contracts`.
