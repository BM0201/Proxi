# @proxi/landing

Aplicación Next.js 15 (App Router) del monorepo Proxi — **landing pública**.

## Desarrollo

```bash
pnpm --filter @proxi/landing dev
```

Corre en el puerto **3003**. Consume los paquetes compartidos `@proxi/ui` y `@proxi/contracts`.
