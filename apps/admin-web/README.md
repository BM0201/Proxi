# @proxi/admin-web

Aplicación Next.js 15 (App Router) del monorepo Proxi — panel de **administración**.

## Desarrollo

```bash
pnpm --filter @proxi/admin-web dev
```

Corre en el puerto **3002**. Consume los paquetes compartidos `@proxi/ui` y `@proxi/contracts`.
