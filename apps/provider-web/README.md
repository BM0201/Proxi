# @proxi/provider-web

Aplicación Next.js 15 (App Router) del monorepo Proxi — app para **proveedores independientes**.

## Desarrollo

```bash
pnpm --filter @proxi/provider-web dev
```

Corre en el puerto **3001**. Consume los paquetes compartidos `@proxi/ui` y `@proxi/contracts`.
