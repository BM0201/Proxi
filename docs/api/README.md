# Documentación de la API

La especificación OpenAPI se genera automáticamente con NestJS/Swagger y está
disponible en `http://localhost:4000/api/docs` al ejecutar la API.

Los contratos compartidos (enums, tipos y DTOs) viven en `packages/contracts`.
